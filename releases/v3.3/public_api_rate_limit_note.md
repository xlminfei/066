# 最终公开验证的API限额处理

首次匿名API查询返回403，响应明确表示x-ratelimit-remaining=0。发布状态没有改变，57项附件已在上传时核对服务器摘要。验证脚本仅在该明确限额条件下用本任务已有Git凭据查询元数据；凭据只在进程内存中，不记录。Release网页和首/尾分片、清单仍无凭据直接下载并核对字节。原脚本和报错日志保留，正式结果以最终remote_delivery_verification.json为准。
