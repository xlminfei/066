# v3.3 请求与范围

本次仅根据用户指定对话“建模方案审查”（6aae89cb-00e4-83ee-8bbb-3aeae4bbede1）的最新审查，处理第四节R端有限尾差数值风险、第六节MeanLogScore说明，以及第八节限定的最终收尾。比较基线是main提交2eec10f0ff37a3fb7c102901d0a3bfaab491440b的v3.2，读取与验证日期2026-09-22。

审查中的高精度反例是a=b=1e-8、区间[.4,.5]。审查未在目标R环境实际运行；本轮先在锁定R4.6.1/rstan2.32.7环境复现，再修改。独立函数参考不直接复制Stan连分式。保持1e-10*max(1,abs(reference))的审计容差，不加概率epsilon、不删极端参数。

保留Bias、图源、支持物种数、模板缓存、后验复算框架、模型/编码、先验、训练权重、固定五折/十折和正式采样参数。此前明确不增加的7.1、7.2及额外新物种/全蛋白接口仍未增加。

用户明确不启动真实研究数据计算。本轮没有新MCMC后验拟合；仅有合成参数的Fixed_param函数接口、确定性/合成替身测试和原有合成后验的身份核验及复算。输入表只作哈希和记录数静态核对。

v3.3独立目录和版本身份。旧v3.2及此前各版本、标签保留。全部本轮源码、说明、数值表、编译探针、复用的原始32个合成fit.rds、失败/成功日志与中间文件纳入完整Release归档；分片和逐文件摘要提供恢复验证。
