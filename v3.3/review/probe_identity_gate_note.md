# 编译对象复用身份核对

最初测试用identical()比较Stan源码字符串时未通过。保存sm@model_code并逐行比较后确认源码内容无差异，192行完全一致；对象字符串附加属性导致严格对象相等不成立。测试改为单一字符串内容相等，同时保留production functions块逐行一致、编译对象SHA-256和生产Stan SHA-256检查。未修改生产Stan或编译对象，没有跳过内容验证。
