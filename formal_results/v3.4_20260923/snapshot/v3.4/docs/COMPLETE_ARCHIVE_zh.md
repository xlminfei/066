# v3.4 完整资料

本轮所有现存源码、说明、冻结基线、测试输入、断言表、旧合成OOF对照数据、中间轮次、成功/失败日志和生成图表均进入完整ZIP，没有按大小或扩展名省略。Git中也保存全部本轮版本文件。

此次只做输入合同/合法结果回归，没有生成或重开原生fit，因而本轮没有新的RDS交付对象。旧32项拟合和此前Beta编译对象仍完整保存在已发布的旧版本Release，未删除、未重标识。本次的reused_evidence只包含实际用于比较的合成OOF表及其来源摘要。

v3.4 Release提供单个完整ZIP、逐文件files.csv、ZIP SHA256、构建读回收据、校验工具和完整Git补丁。无需像前两版大RDS归档那样拼接分片。恢复只涉及下载/校验文件，不会自动启动模型。

打包后才形成的发布过程日志和服务器校验收据独立提供；最终收据写入releases/v3.4/并上传，不递归修改已封存的ZIP或标签。
