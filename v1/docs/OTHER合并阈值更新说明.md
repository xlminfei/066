**2026-09-12修正：M3按固定365物种参考逐位点统计，将出现频率小于1%的氨基酸类别合并为`OTHER`。**

365×1%=3.65。因此出现1、2、3次的类别合并，至少出现4次才保留为独立类别。缺失符号按01原有规则统一为`MISSING`，单独保留；计算1%门槛的参考物种总数为365。

原设置`RARE_MIN <- 3L`配合`frequencies >= RARE_MIN`，会保留出现3次的类别，不符合上述要求。当前[00](00_开始与使用顺序.md)已改为`RARE_MIN <- 4L`；[01](01_输入与编码.md)继续使用同一处设置生成M3编码，02分类与03定量路线共享这套编码，无须分别修改阈值。

`RARE_MIN`表示保留为独立类别所需的最少出现次数。它当前是适用于365物种参考的固定次数门槛，并非随输入自动计算的百分比参数。8物种虚构示例只用于格式与流程练习。若今后另建不同大小的训练参考，应按该版本的物种数重新确定1%门槛并建立字典；使用已发布模型预测新增物种时，应沿用发布时冻结的字典。

**本次实际数据影响。** 核查的是此前确认的365物种`sites.csv`，逐格比较2190个原始位点的M3编码。以下5种组合各出现3次，原来保留为独立类别，现在归入`OTHER`，合计改变15个物种的15个M3单元格：

| 位点 | 氨基酸 | 出现次数 | 占365物种的比例 | 修正后的编码 |
|---|---|---:|---:|---|
| Site3 | L | 3 | 0.822% | OTHER |
| Site20 | G | 3 | 0.822% | OTHER |
| Site196 | S | 3 | 0.822% | OTHER |
| Site315 | C | 3 | 0.822% | OTHER |
| Site315 | S | 3 | 0.822% | OTHER |

同一次检查确认M1、M2编码一致，原始位点字符保持不变。完整变更记录见[实际M3单元格变更](稀有阈值验证/M3_changed_cells.csv)。

**运行身份同步更新。** 当前`MODEL_VERSION`为`ratio_manual_v3_boundary50_rare1pct`，默认`RUN_TAG`为`ratio_v3_20260912_rare1pct_01`。正式分析应从更新后的00、01建立新运行；新设置不会改写既有运行，`resume`只用于恢复相同版本。这样可以利用现有版本检查拒绝旧阈值的准备结果和拟合缓存。

本次没有重新拟合模型。已有M3拟合、留出验证、ROC/AUC、ELPD及预测结果不能直接当作修正后编码的结果，需要在新运行中重新生成。M1、M2的编码和模型公式没有因本次阈值修正而改变；本手册不提供跨版本结果自动迁移。

**已实际完成的验证。** 在本机Docker内的R 4.6.1环境中直接读取修改后手册的设置与完整编码段执行，检查结果为通过：

- 365物种已知计数例中，1、2、3次均合并，4次保留；原3次门槛在同一例子中保留3次类别，复现了原问题。
- 所有约定缺失符号继续单独归入`MISSING`。
- 实际365物种位点表得到上述15处变更，M1/M2及原始位点值保持一致。
- 实际执行手册现有的恢复版本检查，旧版本被拒绝，新版本被接受。
- 交付手册全部42个R命令块通过R解析器语法检查。

汇总见[检查结果](稀有阈值验证/check_summary.json)，语法明细见[R命令块检查](稀有阈值验证/R_code_parse_checks.csv)。这些检查支持编码修正、版本隔离和命令语法结论；不代表已经完成修正后正式研究数据的模型拟合或预测验证。此前的底层拟合证据仍作为历史记录保留。
