这三份文件包含8个虚构物种，其中6个有观测，共60条记录（24计数、24点值、12区间）。
它们是验证报告中20个主模型、分组留出与敏感性检查的输入，不是正式研究数据。
复现输入设置：EXPECTED_SPECIES=8；SEED=20260817；4链；ITER=2000；WARMUP=1000；adapt_delta=0.99。
CV采用species、CV_K=2、CV_SEED=20260817；个别需要改进采样的结果采用ITER=4000、WARMUP=2000、adapt_delta=0.999，详见报告。
照手册在新的运行目录中逐块执行。不同机器或软件版本不保证二进制文件或最后几位数相同。
