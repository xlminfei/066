# v1 精简归档

`v1_figures_model_descriptive.tar.gz` 包含 v1 的模型图、描述性图、可发表 PDF/SVG/PNG、来源表和图注；`v1_derived_key.tar.gz` 包含关键留出、AUC、校准和数值协议表。后验 RDS、编译缓存和运行时复制品没有进入归档，详见 `v1/provenance/omitted_large_artifacts.md`。

